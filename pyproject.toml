[project]
name = "spark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "LAVOINNE Guilhem", email = "lavoinne.guilhem@outlook.fr" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pandas>=3.0.6",
]

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
